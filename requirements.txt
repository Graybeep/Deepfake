# Base runtime deps (gateway, cpu-preprocess, router).
# Heavy ML deps live in requirements-ml.txt so the CPU/gateway images stay small
# and the local test run stays fast.
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
psycopg[binary]==3.2.3
redis==5.2.1
boto3==1.35.90
numpy==2.2.1
python-json-logger==3.2.1
# Multipart form parsing for POST /v1/uploads, the endpoint LocalDiskStorage's
# grant points at. Only exercised on the file:// backend -- with S3/MinIO the
# browser uploads directly to storage and never reaches this service.
python-multipart==0.0.20
